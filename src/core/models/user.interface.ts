export interface IUser {
  id: number;
  name: string;
  event_id: string;
  cardNumber: string;
  cardType: string;
  is_active: boolean;
  show: boolean;
  regions_count: number;
  events_count: number;
  type: string;
  isSelected: boolean;

}
